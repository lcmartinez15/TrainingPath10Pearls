import settings from "./settings";

export const urlAuth = `${settings.urlBase}Auth/signin`;
export const urlGetAllUsers = `${settings.urlBase}User`;
export const urlGetAllCourse = `${settings.urlBase}Course`;
export const urlSearchUdemy = `${settings.urlBase}course/searchUdemy/?search=`;
export const urlChaptersCourseUdemy = `${settings.urlBase}course/searchChapterUdemy?courseId=`;
export const urlLoadUser = `${settings.urlBase}auth`;

export const urlAddUser = `${settings.urlBase}Auth/signup`;

export const urlCategory = `${settings.urlBase}Category`;